# @package _global_

# to execute this experiment run:
# python train.py experiment=example

defaults:
  - override /datamodule: resisc_semisup
  - override /module: cps
  - override /module/network@module.network1: efficientnet
  - override /module/network@module.network2: efficientnet
  - override /paths: trex
  - override /trainer: gpu

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

seed: 12345

datamodule:
  prop: 7
  
module:
  network1:
    weights: IMAGENET1K_V1
  network2:
    weights: IMAGENET1K_V1

paths:
  output_dir: /work/AI4GEO/users/fournip/outputs/resisc/ce_vs_cps/${hydra:runtime.choices.module}/${now:%Y-%m-%d_%H%M%S}